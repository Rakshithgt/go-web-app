apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "go-web-app.fullname" . }}
  labels:
    app: {{ include "go-web-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "go-web-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "go-web-app.name" . }}
    spec:
      containers:
        - name: {{ include "go-web-app.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080

